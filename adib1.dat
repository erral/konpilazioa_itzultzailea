program adibidea is
		a, b, c : integer;
		d, e : float;
     	{* hau lerro
	     anitzeko komentarioa da *}

	procedure gehitu(x, y : in integer) is
	lag : integer;
	{
		lag := a; c := b;
		do {
				{*exit if lag <= 17;*}
				lag := lag-1;
				c := c+1 ;
				exit if (7+6);
		} while lag > 0;
		
	} gehitu;

	{    
		get(a); get(b);
		d := 1/b;
		e := 1/a; 
		{*gehitu(a,b,c);*}
		c := (c*c)*d+e;
		put_line(c*c);
	};
